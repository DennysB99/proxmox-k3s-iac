name: K3s Hardware Runner Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  verify-homelab-connection:
    runs-on: k3s-homelab-pool
    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      - name: Print Execution Confirmation
        run: |
          echo "Pipeline successfully routing into local Proxmox hardware."
          echo "Current execution timestamp: $(date)"